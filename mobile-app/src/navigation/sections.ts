/**
 * Разделы приложения — что их список, что их имена.
 *
 * Раньше и то, и другое жило внутри меню с «тремя линиями». Меню больше нет: разделы стоят
 * внизу постоянной полосой, и список перестал быть частью одного экрана. Поэтому он здесь,
 * в отдельном файле без единого компонента, — его читают и полоса внизу, и шапка, и
 * Главная.
 */

export type Section = "home" | "sterzhen" | "balance" | "screen" | "games" | "rewards" | "more";

/**
 * Как раздел называется в шапке.
 *
 * Список живёт здесь, рядом с самим типом: подпись у кнопки внизу и заголовок сверху — это
 * подпись у кнопки внизу и заголовок сверху — это два разных места, но имя у раздела одно,
 * и расходиться им нельзя. Ровно это и случилось однажды: на карточке Главной раздел звался
 * Sterzhen, а в новой панели внизу — «Стержень».
 */
export const SECTION_TITLES: Record<Section, string> = {
  home: "Главная",
  sterzhen: "Sterzhen",
  balance: "CaloriX",
  screen: "Creker",
  games: "Игры",
  rewards: "Награды",
  more: "Ещё",
};
